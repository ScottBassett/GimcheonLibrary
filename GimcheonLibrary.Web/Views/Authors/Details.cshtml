@model GimcheonLibrary.DataAccess.Models.Author

@{
    ViewData["Title"] = "Details";
}

<h1>@Html.DisplayFor(model => model.Name)</h1>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.About)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.About)
        </dd>
    </dl>
    <h6>List of books to follow...</h6>

    @*<table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(author => author.Books)
            </th>
            
         </tr>

        @foreach (var book in Model.Books)
        {
            <tr>
                <td>
                    <a asp-action="Details" asp-route-id="@book.Id">@Html.DisplayFor(modelItem => book.Title)</a>
                </td>
            </tr>
        }
    </table>*@
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
